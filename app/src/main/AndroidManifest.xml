<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para AdMob -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CreciendoJuntos"
        tools:ignore="GoogleAppIndexingWarning">
        <receiver
            android:name=".home.utils.ReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Meta-data para AdMob -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6126138750630663~5519955991"/>

        <!-- ✅ CORREGIDO: OnboardingActivity con package completo -->
        <activity
            android:name="com.tuusuario.creciendojuntos.onboarding.OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.CreciendoJuntos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ CORREGIDO: MainActivity con package completo -->
        <activity
            android:name="com.tuusuario.creciendojuntos.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.CreciendoJuntos" />

    </application>

</manifest>